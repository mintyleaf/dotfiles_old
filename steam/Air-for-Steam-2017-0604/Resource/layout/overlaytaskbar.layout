resource/layout/overlaytaskbar.layout {

	styles {
	}

	layout {
		place { control=TaskbarPanel height=max width=max }
		place { control=MusicPlayerButton,WebBrowserButton,SettingsButton height=20 y=87 align=right dir=down }

		place { control=SteamLogo,FriendPanel,view_friends,online_friends height=0 width=0 }
	}
}
